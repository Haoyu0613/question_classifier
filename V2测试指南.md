# V2版本测试指南

## 测试目的
验证V2版本（防御性特征识别）是否有效解决了以下问题：
1. ✅ 保持填空形式选择题的改善（12题）
2. ✅ 减少简答题误判（7题→≤2题）
3. ✅ 减少计算题误判（3题→≤1题）
4. ✅ 减少材料分析题误判（3题→≤1题）
5. ✅ 整体准确率提升（89.02%→~91%）

---

## 方法1：使用测试脚本（推荐）

### 步骤1：安装依赖
```bash
cd /home/user/question_classifier
pip install -r question_classifier/requirements.txt
```

### 步骤2：运行快速测试
```bash
python test_v2_classifier.py
```

**预期输出**：
```
测试结果统计
================================================================================
总测试数: 15
正确数: 13-14
错误数: 1-2
准确率: 87-93%

关键改进验证:
  1. 填空形式选择题识别: 3/3 ✅ 改善有效
  2. 简答题误判防护: 2-3/3 ✅ 防护有效
  3. 计算题误判防护: 1-2/2 ✅ 防护有效
  4. 材料分析题误判防护: 1-2/2 ✅ 防护有效
```

### 步骤3：运行完整测试（如果有测试集）
```bash
# 假设你有新题目.xlsx
python your_test_script.py
python analyze_global_impact.py 预测结果.xlsx
```

---

## 方法2：手动验证关键案例

如果无法运行脚本，可以使用Python交互式环境手动测试：

```python
import sys
sys.path.insert(0, 'question_classifier')
from inference import QuestionClassifier

# 加载模型
classifier = QuestionClassifier(model_dir='question_classifier/models')

# 测试1：填空形式选择题（应该识别为选择）
text1 = "The number of giraffes _______ dropping quickly. A. is B. are C. was"
result1 = classifier.predict(text1, use_calibration=True)
print(f"预测: {result1['type']} (期望: 选择)")
print(f"选择题信号: {result1['choice_signals']}")
print(f"关键词: {result1['type_keywords']}")
print()

# 测试2：简答题（不应该被误判为选择）
text2 = "简述A、B、C三个方面的影响，并谈谈你的看法。"
result2 = classifier.predict(text2, use_calibration=True)
print(f"预测: {result2['type']} (期望: 简答)")
print(f"选择题信号: {result2['choice_signals']}")
print(f"关键词: {result2['type_keywords']}")
print()

# 测试3：计算题（不应该被误判为选择）
text3 = "已知A、B、C三点坐标，计算三角形ABC的面积。"
result3 = classifier.predict(text3, use_calibration=True)
print(f"预测: {result3['type']} (期望: 计算)")
print(f"选择题信号: {result3['choice_signals']}")
print(f"关键词: {result3['type_keywords']}")
```

---

## 关键观察点

### ✅ 改善成功的标志
1. **填空形式选择题**被正确识别为"选择"
   - `choice_signals['option_count'] >= 2`
   - `choice_signals['is_blank_choice'] = True`
   - 预测结果 = "选择"

2. **简答题**不被误判为选择题
   - `choice_signals['option_count']` 可能 >= 2（检测到ABC）
   - `type_keywords['has_jianda'] = True`（检测到"简述"）
   - 预测结果 = "简答"（防护生效）

3. **计算题**不被误判为选择题
   - `choice_signals['option_count']` 可能 >= 2（检测到ABC点）
   - `type_keywords['has_jisuan'] = True`（检测到"计算"）
   - 预测结果 = "计算"（防护生效）

### ⚠️ 需要注意的情况
1. 如果简答题仍然被大量误判（>50%）
   → 关键词库可能需要扩充

2. 如果填空形式选择题改善消失
   → 抑制逻辑可能太强，需要调整

3. 如果整体准确率<90%
   → 需要重新调整策略

---

## 测试结果判断标准

### ✅ V2成功的标准
- 整体准确率 ≥ 90.5%
- 简答题误判 ≤ 2个（相比V1的7个）
- 计算题误判 ≤ 1个（相比V1的3个）
- 材料分析误判 ≤ 1个（相比V1的3个）
- 填空形式选择题改善保持（3/3或12/12）

### ⚠️ 需要调整的情况
- 整体准确率 90-90.5%之间
- 简答题误判 3-5个
- 建议：微调抑制因子或扩充关键词

### ❌ V2失败的情况
- 整体准确率 < 90%
- 简答题误判 > 5个
- 填空形式选择题改善丢失
- 建议：回滚到旧版本或重新设计

---

## 快速诊断命令

### 检查V2代码是否正确部署
```bash
cd /home/user/question_classifier/question_classifier

# 检查type_keywords.py是否存在
ls -l type_keywords.py

# 检查inference.py是否有防御性识别
grep -n "should_suppress_choice_boost" inference.py

# 检查导入语句
grep -n "from type_keywords import" inference.py
```

### 查看当前版本
```bash
git log --oneline -3
# 应该看到: 884175e Add comprehensive global impact analysis
#          8754da4 Add defensive feature recognition
```

---

## 问题排查

### 问题1：ModuleNotFoundError: No module named 'type_keywords'
**原因**：运行目录不正确或导入路径问题

**解决方案**：
```bash
# 确保在正确目录
cd /home/user/question_classifier

# 或在脚本中添加路径
import sys
sys.path.insert(0, 'question_classifier')
```

### 问题2：简答题仍然被误判
**原因**：关键词未覆盖该题目的表述方式

**解决方案**：
在 `type_keywords.py` 中扩充关键词：
```python
JIANDA_KEYWORDS = re.compile(r'(简述|简要说明|简答|阐述|说明.*原因|谈谈|分析.*影响|列举|举例说明|你认为|你的看法|请回答)')
```

### 问题3：填空形式选择题改善消失
**原因**：抑制逻辑过强

**解决方案**：
在 `inference.py` 中调整抑制条件：
```python
# 只在2选项时抑制，3+选项不抑制
if option_count <= 2:
    if (has_jianda or has_jisuan or has_cailiao):
        return True
```

---

## 对比V1和V2

如果想明确看到V2的改进，可以：

```bash
# 保存V2结果
python your_test_script.py
cp 预测结果.xlsx 预测结果_V2.xlsx

# 切换到V1
git checkout 275d5f3
python your_test_script.py
cp 预测结果.xlsx 预测结果_V1.xlsx

# 切回V2
git checkout 884175e

# 对比
python analyze_global_impact.py 预测结果_V1.xlsx
python analyze_global_impact.py 预测结果_V2.xlsx
```

---

## 下一步行动

根据测试结果：

### 如果V2效果好（准确率≥90.5%）
1. ✅ 提交最终版本
2. ✅ 更新报告给导师
3. ✅ 合并到主分支

### 如果V2效果一般（准确率90-90.5%）
1. 🔧 微调参数
2. 🔧 扩充关键词
3. 🔧 重新测试

### 如果V2效果差（准确率<90%）
1. ⚠️ 分析失败原因
2. ⚠️ 考虑混合策略
3. ⚠️ 或回滚到旧版本

---

**测试完成后，请告诉我结果，我们可以根据实际情况进一步优化！**
